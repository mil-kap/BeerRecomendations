import React from "react"
import { Beers } from "../beers-list-page/Beers"

export const ManageBeers = React.memo(() => {
    // TODO: Add reordering - drag and drop
    
    return (
        <Beers></Beers>
    )
})